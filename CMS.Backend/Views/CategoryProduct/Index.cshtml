@model IEnumerable<CMS.Data.Entities.CategoryProduct>
@{
    ViewData["Title"] = "Danh mục sản phẩm";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="page-header">
    <div class="page-title-group">
        <h1 class="page-title">🏷️ Danh Mục Sản Phẩm</h1>
        <p class="page-subtitle">Phân loại sản phẩm thể thao theo nhóm</p>
    </div>
    <a asp-action="Create" class="btn-sport btn-primary-sport">
        <i class="bi bi-plus-lg"></i> Thêm danh mục
    </a>
</div>

<div class="data-card">
    <div class="data-card-header">
        <div class="data-card-title">
            <span class="title-icon"><i class="bi bi-tags"></i></span>
            Tất cả danh mục sản phẩm
            <span class="badge-admin badge-primary" style="margin-left:6px;">@Model.Count() danh mục</span>
        </div>
    </div>
    <div style="overflow-x:auto;">
        @if (!Model.Any())
        {
            <div class="empty-state">
                <span class="empty-state-icon">🏷️</span>
                <div class="empty-state-title">Chưa có danh mục sản phẩm</div>
                <div class="empty-state-text">Tạo danh mục để nhóm các sản phẩm thể thao</div>
                <a asp-action="Create" class="btn-sport btn-primary-sport" style="margin-top:16px;display:inline-flex;">
                    <i class="bi bi-plus-lg"></i> Tạo ngay
                </a>
            </div>
        }
        else
        {
            <table class="admin-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Tên danh mục</th>
                        <th>Mô tả</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        string[] icons = { "👟", "👕", "🏋️", "⚽", "🏀", "🎾", "🚴", "🥊", "🏊", "⛷️" };
                        int idx = 0;
                    }
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><span class="id-pill">#@item.Id</span></td>
                            <td>
                                <div style="display:flex;align-items:center;gap:10px;">
                                    <div style="width:34px;height:34px;border-radius:8px;background:rgba(0,212,255,0.1);border:1px solid rgba(0,212,255,0.2);display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0;">
                                        @icons[idx % icons.Length]
                                    </div>
                                    <span style="color:var(--text-primary);font-weight:600;">@item.Name</span>
                                </div>
                            </td>
                            <td style="max-width:280px;">
                                <span style="font-size:13px;color:var(--text-muted);display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">
                                    @(string.IsNullOrEmpty(item.Description) ? "—" : item.Description)
                                </span>
                            </td>
                            <td>
                                <div style="display:flex;gap:6px;">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn-icon btn-warning-sport" title="Sửa">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.Id"
                                       class="btn-icon btn-danger-sport" title="Xóa"
                                       onclick="return confirm('Xóa danh mục sản phẩm «@item.Name»?')">
                                        <i class="bi bi-trash3"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                        idx++;
                    }
                </tbody>
            </table>
        }
    </div>
</div>
