@model CMS.Data.Entities.Banner

@{
    ViewData["Title"] = "XÃ³a Banner";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid">
    <h1 class="page-title text-danger mb-4">XÃ³a Banner</h1>

    <div class="data-card mb-4">
        <div class="card-body">
            <h3>Báº¡n cÃ³ cháº¯c cháº¯n muá»‘n xÃ³a Banner nÃ y?</h3>
            <p class="text-muted">HÃ nh Ä‘á»™ng nÃ y khÃ´ng thá»ƒ hoÃ n tÃ¡c.</p>
            <hr />
            <dl class="row mt-3">
                <dt class="col-sm-3">TiÃªu Ä‘á»</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Title)</dd>
                
                <dt class="col-sm-3">HÃ¬nh áº£nh/Video</dt>
                <dd class="col-sm-9">
                    @if (!string.IsNullOrEmpty(Model.ImageUrl)) {
                        if (Model.ImageUrl.EndsWith(".mp4")) {
                            <video src="@Model.ImageUrl" width="200" autoplay muted loop></video>
                        } else {
                            <img src="@Model.ImageUrl" width="200" class="img-thumbnail" />
                        }
                    } else {
                        <span>KhÃ´ng cÃ³</span>
                    }
                </dd>

                <dt class="col-sm-3">ÄÆ°á»ng dáº«n URL</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.LinkUrl)</dd>
                
                <dt class="col-sm-3">Thá»© tá»± hiá»ƒn thá»‹</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.DisplayOrder)</dd>
            </dl>

            <form asp-action="Delete" class="mt-4">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="XÃ³a vÄ©nh viá»…n" class="btn-sport btn-danger-sport" /> |
                <a asp-action="Index" class="btn-sport btn-secondary-sport">Há»§y bá»</a>
            </form>
        </div>
    </div>
</div>


